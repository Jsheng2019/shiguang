services:
  - type: web
    name: shiguang-backend
    runtime: node
    region: singapore
    buildCommand: cd backend && npm ci --include=dev && npm run build
    startCommand: cd backend && node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
